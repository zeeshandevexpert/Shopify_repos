{%- layout none -%}
{%- capture variants -%}{%- for variant in product.variants -%}{
{{ "id" | json }}:{{ variant.id | json }},
{{ "qty" | json }}:{{ variant.inventory_quantity | default: 0 | json }}
}{% unless forloop.last %},{% endunless %}{%- endfor -%}{%- endcapture -%}
[{{ variants | strip | strip_newlines  }}]